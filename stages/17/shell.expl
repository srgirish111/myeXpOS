int main()
{
decl
    int temp;
    string a;
enddecl
begin
    temp = exposcall ("Read", -1,  a);
    temp = exposcall ("Exec" , a);
    if(temp== -1)then
		temp=exposcall("Write",-2,"not found");
	endif;
    return 0;
end
}